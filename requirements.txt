# Common dependencies (all platforms)
requests
PyQt6
psutil
discord.py-self
aiohttp
pyautogui
pynput
websockets
pyperclip

# Windows-specific (toast notifications and Win32 API)
win11toast; sys_platform == 'win32'
pywin32; sys_platform == 'win32'

# macOS-specific (Quartz, Foundation, etc.)
pyobjc; sys_platform == 'darwin'